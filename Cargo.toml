[package]
name = "portable-io"
version = "0.0.3-dev"
edition = "2018"
license = "MIT OR Apache-2.0"
repository = "https://github.com/brodycj/portable-io"
description = """"
A subset of Rust `std::io` functionality supported for `no-std`.
"""

[dependencies]
libc = { version = "0.2.169", optional = true, default-features = false }
rustversion = "1.0.19"

[features]
# TODO: finer-grained feature options
# XXX TODO FIX DOCUMENTATION OF ALLOC FEATURE LOCALLY & ON docs.rs
# XXX TBD FEATURE DEPENDENCY - alloc -> readbuf - ??? ???
alloc = ["readbuf"]
# XXX TODO FIX DOCUMENTATION OF THIS UNSTABLE FEATURE:
readbuf = []
# TODO: DOCUMENT AS UNSTABLE (POSSIBLY UNSUPPORTED) FEATURE with some MISSING FUNCTIONALITY (with known panics)
os-error = []
# TODO: DOCUMENT AS UNSTABLE (POSSIBLY UNSUPPORTED) FEATURE FOR UNIX PLATFORMS ONLY
unix-iovec = ["dep:libc"]
